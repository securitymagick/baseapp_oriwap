CREATE TABLE menu (
  id         INTEGER PRIMARY KEY,
  menuName VARCHAR(32),
  menuItem INTEGER,
  menuHeading VARCHAR(32),
  menuUrl VARCHAR(32),
  menuGlyph VARCHAR(32),
  menuPhrase VARCHAR(64),
);